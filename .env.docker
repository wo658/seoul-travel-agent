# Docker Environment Configuration
# Use this for docker-compose setup

ENVIRONMENT=docker

# Database (PostgreSQL in Docker)
DATABASE_URL=postgresql://postgres:postgres@db:5432/seoul_travel

# API URLs
EXPO_PUBLIC_API_URL=http://backend:8000
